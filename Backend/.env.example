PORT=3000
MONGODB_URI=mongodb://localhost:27017/progresstracker
JWT_SECRET=your_jwt_secret_here
# Timezone for the midnight daily-task provisioning cron comes from OrgSettings.timezone
# (PUT /api/calendar/settings), not an env var — defaults to Asia/Kolkata.
